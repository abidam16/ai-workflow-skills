interface:
  display_name: "ADR Writer"
  short_description: "Create or supersede architecture-aware ADRs with explicit architecture linkage and next-step routing."
  brand_color: "#8B5CF6"
  default_prompt: "Use the adr-writer skill to record this decision as an ADR and include architecture linkage plus a concrete next step."
policy:
  allow_implicit_invocation: true
