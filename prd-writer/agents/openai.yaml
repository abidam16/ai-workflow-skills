interface:
  display_name: "PRD Writer"
  short_description: "Create or update architecture-aware product requirements documents."
  default_prompt: "Use $prd-writer to create or update PRD.md with product truth, architecture impact, downstream handoff, and a concrete next step."
policy:
  allow_implicit_invocation: true
