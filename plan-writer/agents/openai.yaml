interface:
  display_name: "Plan Writer"
  short_description: "Creates architecture-aware single-task implementation plans."
  default_prompt: "Use plan-writer to create or update one architecture-aware PLAN.md for the next bounded implementation task."
policy:
  allow_implicit_invocation: true
