interface:
  display_name: "Roadmap Planner"
  short_description: "Creates architecture-aware product and initiative roadmaps with concrete plan handoffs."
  default_prompt: "Use the roadmap-planner skill to create or update the roadmap. Respect PRD, ARCHITECTURE.md, ADRs, and the shared workflow contracts."
policy:
  allow_implicit_invocation: true
